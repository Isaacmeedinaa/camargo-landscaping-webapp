# camargo-landscaping-webapp

In order to run this app do the following

On your terminal type:

- "npm i"

then:

- "npm i nodemon"

To run the app on the local server type in terminal:

- "nodemon app.js"